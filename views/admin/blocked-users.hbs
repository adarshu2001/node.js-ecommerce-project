<section id="all-users">
    <h3>Blocked Users</h3>
    <div class="board">
        <table width="100%">      
            <thead>
                <tr>
                    <td>Name</td>
                    <td>Phone</td>
                    <td>Status</td>
                    <td>Action</td>
                </tr>
            </thead>
            <tbody>
                {{#each blockedUSers }}
                <tr>
                    <td class="people">
                        <i class="fas fa-user"></i>
                        <div class="p-details">
                            <h5>{{this.name}}</h5>
                            <p>{{this.email}}</p>
                        </div>
                    </td>
                    <td class="phone">
                        <p>{{this.mobile}}</p>
                    </td>
                    <td class="status">
                        {{#if this.status}}
                        <p>Active</p>
                        {{else}}
                        <p>Blocked</p>
                        {{/if}}
                    </td>
                    <td class="action">
                        
                        <a class="unblock" href="/admin/unblock-user/{{this._id}}">Unblock</a>
                    
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</section>